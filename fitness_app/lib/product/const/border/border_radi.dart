import 'package:flutter/widgets.dart';

@immutable
final class BorderRadi {
  ///  extremeLow 6 radius
  static const BorderRadius extremeLowCircular = BorderRadius.all(Radius.circular(6));

  ///  low 12 radius

  static const BorderRadius lowCircular = BorderRadius.all(Radius.circular(12));

  ///  normal 16 radius

  static const BorderRadius midCircular = BorderRadius.all(Radius.circular(16));
}
